def ifAnyGuessed(lettersGuessed, secretWord):
    '''
    Reads in the guessed letter and if its part of the letters,
    prints appropriate statement and function call
    '''
    
    for char in lettersGuessed:
        if char in list(secretWord) and not in :
           return print("Good guess: ", getGuessedWord(secretWord, lettersGuessed))
        else:
            return 
    
